# clippy-code Environment Configuration
# Copy this file to .env and fill in your API keys

# OpenAI (required for OpenAI models)
OPENAI_API_KEY=sk-...

# Cerebras (for fast inference with Llama models)
CEREBRAS_API_KEY=csk-...

# Together AI (alternative LLM provider)
TOGETHER_API_KEY=...

# Groq (ultra-fast inference)
GROQ_API_KEY=gsk_...

# DeepSeek (specialized coding models)
DEEPSEEK_API_KEY=...

# Ollama (local models - usually doesn't need a key)
# OLLAMA_API_KEY=ollama

# Optional: Override default model
# CLIPPY_MODEL=gpt-4o

# Optional: Override default base URL
# OPENAI_BASE_URL=https://api.cerebras.ai/v1

# Optional: Limit max tokens for responses (default: use API's default)
# Only set this if you want to enforce a specific limit
# CLIPPY_MAX_TOKENS=8192
